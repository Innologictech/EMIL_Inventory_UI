import { DispatchScannedItemsComponent } from './dispatch-scanned-items.component'

describe('DispatchScannedItemsComponent', () => {
  it('should mount', () => {
    cy.mount(DispatchScannedItemsComponent)
  })
})