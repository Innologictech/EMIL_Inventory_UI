import { PendingDeliveriesComponent } from './pending-deliveries.component'

describe('PendingDeliveriesComponent', () => {
  it('should mount', () => {
    cy.mount(PendingDeliveriesComponent)
  })
})